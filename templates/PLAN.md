# Plan

Single ranked backlog, most important first. Entries are **deleted** when done, never annotated —
a plan that accumulates completed items stops being read.

<!--
Each entry: a heading, a type tag with importance and effort, then one paragraph. No design.
Anything needing more than a paragraph needs an ADR (if it is a choice) or a task note (if it is
work in progress).

  Type       bug | debt | feature | docs
  Importance low | medium | high  — what it costs to keep not doing this
  Effort     low (under a day) | medium (under a week) | high (larger, or not yet known)

Technical debt is a type tag here, not a separate file. The debt-versus-feature trade-off can only
be made inside one ordered list.

Delete this comment and the example below once real entries exist.
-->

### Short title, imperative

*Type: feature — Importance: medium — Effort: low*

One paragraph on what and why. Name what it unblocks, or what it costs to keep not doing. If the
entry is blocked on another, say which.
